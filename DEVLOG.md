# Devlog

Running log of work on this project: what's done, what's in progress, and what's next.

## Done

- **2026-07-19** Scaffolded the Frosthaven companion app: React + Vite (TS) frontend, Hono-based Worker API at `worker/index.ts`, wired together with `@cloudflare/vite-plugin`. Single Worker deploys both static assets and API (`/api/*` routed to the Worker, everything else SPA fallback).
- **2026-07-19** `git init`'d the repo on `main`, initial commit made.
- **2026-07-19** Created GitHub repo [siderbx/frosthaven-companion](https://github.com/siderbx/frosthaven-companion) via `gh`, pushed initial commit. Later switched from private to **public** at user's request.
- **2026-07-19** Connected Cloudflare Workers Builds (native Git integration) in the dashboard. Verified live and working: https://frosthaven-companion.mcarlton.workers.dev/ serves the SPA and `/api/health` responds `{"status":"ok"}` from the Worker. Full pipeline confirmed: push to `main` → Workers Builds → auto-deploy.
- **2026-07-19** Repo switched to **public** at user's request (`gh repo edit --visibility public`).
- **2026-07-19** Shipped the first real feature: a single-character companion for the **Human Voidwarden (Jaws of the Lion)**, tailored to the user's current game. Four tabs:
  - **Character** — editable name, level/XP/gold counters, current/max HP with a visual bar. Replaces the cardboard character card.
  - **Perks** — freeform checklist the user populates from their physical perk sheet; check off as taken.
  - **Modifier Deck** — simulates the standard 20-card base attack modifier deck (draw/discard, auto-reshuffle on the crit/miss cards), plus a "Customize deck" editor so perk-driven deck changes (remove a −1, add a +1, etc.) can be reflected.
  - **Action Cards** — user enters their 11 Voidwarden cards once (name/initiative/top/bottom/tags/loss flags); tracks hand/used/lost status through short/long rests, computes round initiative as the min of the two played cards (per the standard rule), and suggests pairings by tag complementarity.
  - All state persists to `localStorage` so it survives reloads on the iPad.
  - Pushed via the pipeline and confirmed live: deployed JS bundle at https://frosthaven-companion.mcarlton.workers.dev/ contains the new UI (verified via curl, not just a guess), `/api/health` still responds correctly.
- **2026-07-19** User supplied a photo of the physical Voidwarden character mat, confirming HP by level (1–9: 6/7/8/9/10/11/12/13/14), hand size 11, and XP thresholds (45/95/150/210/275/345/420/500). Added `src/data/voidwarden.ts` with this verified table and wired **Max HP to auto-fill from Level** in the Character tab (still manually overridable for items/effects that boost it). When leveling up: if the character was at full HP, they stay full at the new max; otherwise current HP just gets clamped to the new max, matching how the physical mat is used. Tested both cases in-browser before shipping. Also added an **XP-to-next-level hint** on the Experience counter using the verified thresholds.
- **2026-07-19** User clarified the real JOTL leveling mechanic (core Gloomhaven-style: pick one new card per level-up), correcting an earlier wrong assumption (drawn from the card site's "Build Mode," which doesn't actually enforce unlock gating). Investigated gloomhavencards.com's card art directly (zoomed into each of the 14 Voidwarden card images) and found a crown icon on every card: **11 cards marked "1"** (the full starting hand — matches hand size exactly, no choice needed) and **3 cards marked "X"** (Cold Embrace, Resigned Frenzy, Sap Warmth — the level-up bonus pool). Couldn't verify the exact level each "X" card unlocks at (not printed as a number, and no reliable secondary source), so rather than guess:
  - Added a **`reserve`** status to `ActionCardStatus` (`reserve | hand | used | lost`) representing "owned but not in active hand."
  - Seeded all 14 cards in `src/data/voidwarden.ts` (`buildVoidwardenActionCards`) — the 11 starting cards go straight to Hand, the 3 X-cards start in Reserve.
  - Added **Reserve ↔ Hand** swap buttons in the Action Cards tab so the user moves each X-card over whenever their rulebook/level-up confirms they've unlocked it — sidesteps the unverified exact-level question entirely while staying mechanically accurate.
  - Card names + initiative values are pre-filled (verified from the card art); top/bottom ability text is left blank for the user to paraphrase themselves — deliberately not transcribing Cephalofair's exact card wording into the public repo (see prior note on this).
  - Seeded the **Perks** tab from the user's photographed perk sheet (`src/data/voidwardenPerks.ts`), expanding repeatable perks (e.g. "Replace one +0 card..." ×3) into individual checkbox rows to match the physical sheet.
  - Verified all of this in-browser (JS-dispatched clicks, since the browser automation's synthetic click occasionally no-ops — confirmed via console that it's a tooling quirk, not an app bug) before shipping.
- **2026-07-19** **Correction from the user**: the above entry's "3 X-cards are gained via level-up" framing was wrong. All 14 cards — both "1" and "X" crown — are available from level 1; there's no per-level card unlocking in JOTL at all. The "1"/"X" distinction on the card art means something else (not determined). Fixed:
  - `buildVoidwardenActionCards()` now gives every card `level: 1` (previously the 3 Reserve cards had a placeholder `level: 0`).
  - Reserve section copy changed from "Cards you've unlocked but haven't chosen..." to "All 14 cards are available from level 1 — these are just the ones not currently in your hand of 11."
  - The Reserve/Hand split itself didn't need to change — it's still mechanically correct (hand size 11 < pool 14 means *some* split is always needed), it just isn't gated by level; the default split is an arbitrary starting point, freely rearrangeable via the swap buttons.
- **2026-07-19** User clarified the real Perks mechanic, correcting the previous "one perk per level-up, boxes = how many times repeatable" assumption: **perks are earned two ways** — leveling up lets you pick any perk outright (no box requirement), and completing Battle Goals lets you spend checkmarks on a *specific* perk's boxes (the box count printed next to each perk is how many Battle Goal checkmarks that perk needs). Reworked accordingly:
  - `Perk` type changed from `{ checked }` to `{ boxesRequired, boxesFilled, checked }`.
  - `voidwardenPerks.ts` reverted from the wrongly-expanded 15-row list back to the correct **11 perks**, each with its real box count (1, 1, 2, 3, 1, 2, 1, 1, 1, 1, 1).
  - `PerkList` now renders clickable box pips per perk (fills toward `boxesRequired`, auto-checks "Taken" when full) plus an independent "Taken" toggle for the level-up path — tested both: filling a 1-box perk auto-checks it, filling 1-of-2 on a 2-box perk correctly leaves it unchecked.
- **2026-07-19** User provided an authoritative source — the [m-ender/gloomhaven-rules](https://github.com/m-ender/gloomhaven-rules) repo, a full text transcription of the official Gloomhaven rulebook. Read the primary source directly (downloaded `README-no-images.md`, grepped for the exact passages) rather than relying on a lossy fetch summary, since the file is ~135KB and a summarizing fetch had missed the key section entirely on the first attempt. This settled two things definitively:
  - **The Perks rework above was still wrong.** The actual rule (p.44-45, quoted verbatim): *"when leveling up, the player can mark **one** of the perk boxes... If a perk has multiple check boxes next to it, this means that the perk can be gained multiple times for the cost of one perk each"* and *"For every three checkmarks a character earns, he or she immediately gains an additional perk... A character can achieve a total of six additional perks in this way."* So it's not "box count = Battle Goal cost for that specific perk" (what got built two entries up) — it's: **every level-up (uncapped) and every 3 Battle Goal checkmarks (capped at 6 perks) each grant one interchangeable "perk pick,"** spendable on *any* perk box, and a perk with N boxes can simply be picked N separate times. Reworked again:
    - `Perk` type simplified to `{ timesAvailable, timesTaken }` — no more artificial "battle-goal-cost vs level-up" distinction, no separate "Taken" toggle.
    - Added `battleGoalCheckmarks` to `CharacterState` with a counter + hint ("N more for a perk pick") on the Character tab, matching the rulebook's "notes section... includes a track for checkmarks."
    - Verified in-browser: clicking a perk's box increments `timesTaken`, header shows "picks spent / picks total" across all perks.
  - **Confirms the Action Cards fix from two entries up was right all along**: *"each character also has immediate access to three Level X cards"* (p.45) — exactly matches our 14-card pool (11 Level-1 + 3 Level-X) and the Reserve/Hand design, no further changes needed there.
- **2026-07-19** User suggested also checking the image version of the rulebook, since the text has callout markers ("(a)", "(b)", etc.) pointing at diagrams that don't come through in a text-only fetch. Downloaded `img/rulebook/character-sheet.png` and `img/rulebook/battle-goal-card.png` directly and viewed them. This visually confirmed the Perks fix from the entry above with no further changes needed: the character sheet's notes section has a labeled area (i) showing exactly **six groups of three checkboxes** — matching "every 3 checkmarks earns a perk, up to 6 perks that way" one-to-one. The Battle Goal card shows a single checkmark icon per card. Our `battleGoalCheckmarks` running-counter model is mathematically equivalent to the sheet's 6×3 checkbox grid.
- **2026-07-19** User asked for the checkmark-spending "future polish" above to actually happen: picking a perk should record whether it came from a level-up or from points, and a points-sourced pick should subtract from the Battle Goal Checkmarks total. Implemented:
  - `Perk.picks` is now `PerkPickSource[]` (`'level' | 'points'`, one entry per pick spent) instead of a plain `timesTaken` count — each box remembers its own source.
  - Tapping the next empty box on a perk opens an inline "Take via: Level / Points (−3, have N) / Cancel" chooser. The Points button disables itself when `battleGoalCheckmarks < 3`.
  - Confirming "Points" deducts 3 from `CharacterState.battleGoalCheckmarks` (clamped to 0); confirming "Level" costs nothing.
  - Boxes render blue when picked via Level, green via Points, so the source is visible at a glance without re-opening the chooser.
  - Tapping a filled box undoes that specific pick and — critically — **refunds** the 3 checkmarks if it was Points-sourced, keeping the running total reversible.
  - Verified all three flows in-browser: Level pick (no deduction), Points pick (5 → 2 checkmarks), undo of a Points pick (2 → 5, refunded correctly).
- **2026-07-20** Added class keywords **Arcane · Educated · Outcast** next to "Human Voidwarden" on the Character tab (`VOIDWARDEN_KEYWORDS` in `voidwarden.ts`), from the character mat's Notes section.
- **2026-07-20** **Found and fixed a real Modifier Deck bug**, reported by the user as "looks broken" after going through the deck and reshuffling. My first pass (draw 25x rapidly, edit composition, add card types) didn't catch it because I only checked for crashes/visual glitches, not card conservation. The user sent a screenshot showing "1 left, 0 discarded" — should never happen with a 20-card deck — which pointed straight at the bug: in `drawCard()` (`lib/modifierDeck.ts`), when a reshuffle-triggering card (crit `x2` or miss `Null`) was drawn *before* the draw pile was empty, the reshuffle branch did `shuffle(nextDiscard)` — only the discard pile plus the just-drawn card — and silently dropped `rest`, the remaining un-drawn cards still in hand. Every such reshuffle permanently destroyed however many cards hadn't been drawn yet, which is exactly how a fresh 20-card deck degrades to "1 left" after a normal play session. Fixed to `shuffle([...rest, ...nextDiscard])`. Verified with an 80-draw automated soak test (card total stayed at exactly 20 throughout, including multiple forced reshuffles) and a second test interleaving manual "Shuffle now" clicks with draws — also held at 20 throughout.
  - **Lesson for future testing**: when a stateful pile/deck is involved, always assert on the *conservation invariant* (total cards in all piles combined stays constant), not just "did it crash" — that's what would have caught this the first time.
- **2026-07-20** Added a **Mastery** section to the Perks tab. Per the Todo item, the exact mastery text and condition icons from the user's character-mat photo weren't confident enough to hardcode — checked the official FAQ (cephalofairgames.github.io/frosthaven-faq) first to confirm the general mechanic (a mastery must be completed entirely within one scenario that the party wins; Scenario 0 doesn't count; exhausting mid-scenario doesn't forfeit a mastery already fulfilled that session) and used that for the panel's hint text, but left the two mastery conditions themselves as **freeform, user-editable rows** (text input + achieved checkbox), matching the existing Perks/Action Cards precedent of not asserting unverified Cephalofair card/mat text. New `Mastery` type in `types.ts`, new `MasteryList.tsx` component, persisted to `localStorage` under `fh-masteries`, seeded with 2 blank rows (every character mat has exactly 2). Verified in-browser: typing into a mastery's text field and toggling its achieved box both work and persist.
- **2026-07-20** **Round initiative now comes from the first card picked (the leading card), not `min()`** (user request — "sometimes you want the higher number to go later"). This corrected a real rules bug: the app had been computing initiative as the minimum of the two played cards, but per the rulebook players reveal their cards with a *leading card* of their choice on top and that card's value is their initiative — going late deliberately is a legitimate tactic the old `min()` made impossible. (The 2026-07-19 note calling min "the standard rule" was wrong.) Implementation: a `pickOrder` state array tracks selection order through a shared `pickSlot()` handler (side buttons + suggestion chips); the earliest still-selected pick is the lead, replacing/deselecting keeps the order chronologically correct, and a **Swap lead** button in the round panel flips it for when you picked in the wrong order. The Initiative slot now shows the value plus "lead: <card>". Verified in-browser: picked 89-init card first + 49-init bottom → initiative showed 89 (old code would say 49); Swap lead flipped it to 49; Clear resets everything. `tsc` + `oxlint` clean.
- **2026-07-20** **Selected cards now float to the top of the Hand list** (user request) — cards picked as this round's top/bottom sort above the rest of the hand for quick reference while resolving the round; unselected cards keep initiative order, and deselecting restores the original order. One-line comparator change in `ActionCards.tsx`. Verified in-browser both ways (initiative-last card jumped to first when selected as top; a bottom-selection floated too; clearing the selection restored initiative order). During verification a stray automation click accidentally hit a card's remove "×" and deleted Freeze the Soul from stored state — restored it by re-adding a blank-text card of that name and reloading, which doubled as a live end-to-end test of the `withVoidwardenCardText` migration (it refilled text/tags/loss flags on mount as designed). `tsc` + `oxlint` clean.
- **2026-07-20** **Built persistent-effect tracking (active area) for cards whose effects span turns** (user request, follow-on to the card-text work). Three Voidwarden halves are tracked persistents per the verified scans: Signs of the Void bottom (5-slot track → lost; XP on 3rd/5th), Sap Warmth top (3-slot track → *discarded*, that half has no loss icon), Master Influence top (whole-scenario, no slots → lost). Implementation: new `'active'` status + `activeCharges`/`activeHalf` on `ActionCard`, new `PersistentEffect` type (`charges: number | null`, `endsIn: 'used' | 'lost'`, `note`), configs live on `VOIDWARDEN_CARD_DETAILS` (looked up by name via `persistentEffectFor()` at play time — deliberately *not* stored on cards, so no migration and data fixes apply retroactively; custom cards fall through to the normal flow). `confirmRound` now routes a played persistent half to the Active effects panel: charge pips + "Use charge" button (auto-moves the card to its end pile when the last slot is spent), whole-scenario effects show "Lasts all scenario", and every active card has a manual "End effect". Rests already only touch `used`, so active cards correctly survive resting. Verified all paths in-browser: 5-charge track counted down and landed in Lost; all-scenario effect ended manually to Lost; 3-charge track landed in Used not Lost; one-shot loss halves and normal halves unaffected. `tsc` + `oxlint` clean.
- **2026-07-20** **Filled in Action Card top/bottom text for all 14 Voidwarden cards** (Todo item). Source: the card scans behind gloomhavencards.com — the `cmlenius/gloomhaven-card-browser` GitHub images branch (added to References). Downloaded the 14 scans to the session scratchpad as read-only reference (deliberately *not* committed to the repo — redistributing Cephalofair's card art is what the paraphrase approach avoids), read each card, and for every ambiguous consume/infuse element icon cropped + 4x-upscaled the icon region to identify it (this caught real details: e.g. Freeze the Soul consumes *Ice* on top but *Dark* on bottom; Grasp of Doom's top *infuses* Ice+Dark rather than consuming; Suggestion's Stun rider consumes Ice, not Dark). Wrote paraphrased mechanics-only summaries (own wording, no verbatim card text, no flavor) into a new `VOIDWARDEN_CARD_DETAILS` table in `voidwarden.ts`, including verified loss flags (9 loss halves: Signs of the Void bottom, Suggestion top, Grasp of Doom top, Turn Out the Lights bottom, Lure of the Void bottom, Master Influence top, Resigned Frenzy top, Sap Warmth bottom, Cold Embrace top) and pairing tags. `buildVoidwardenActionCards()` now seeds from this table, and a new `withVoidwardenCardText()` migration (run once on App mount) fills blanks on already-stored cards — only blank fields are touched, so user-entered text survives; loss flags ride along with their half's text fill; idempotent. Verified in-browser against live localStorage: all 14 stored cards got text/tags, loss halves render with the 🔥 marker, and a JS probe confirmed 0 missing texts and exactly the 9 expected loss flags. Scratchpad scans deleted after use. `tsc` + `oxlint` clean.
- **2026-07-20** **Verified Short Rest / Long Rest against the rulebook** (`m-ender/gloomhaven-rules`, Resting p.17 — Frosthaven's resting rules are unchanged from Gloomhaven) and corrected `ActionCards.tsx`, which had both rests wrong. The rulebook: a rest needs **2+ cards in the discard pile** and **always loses exactly one card**. **Short rest** loses one card at **random** (not chosen), with a one-time option to **suffer 1 damage to randomly lose a *different* card** instead — there is no "no loss" option, and the modifier deck is not involved at all (the old help text invented a "if you drew a −1/−2" modifier-deck mechanic that doesn't exist; that was the DEVLOG's own hypothesis, now disproven). **Long rest** loses one **chosen** discarded card, returns the rest to hand, then does **Heal 2 Self + refresh spent items**. What the app did before: short rest let you *pick* which card to lose (or none); long rest *recovered a lost card* — a completely fabricated mechanic — and lost nothing. Rewrote both: short rest now rolls a random loss into a confirm/reroll picker (reroll disappears after one use, enforcing once-per-rest), long rest is a choose-one-to-lose picker with hint text for the Heal 2 + item refresh (HP/items are tracked on other tabs, so those stay manual). Both buttons now require `used.length >= 2`. Verified the full flow in-browser: short rest random-loss → reroll switched the doomed card and vanished → confirm moved it to Lost and returned the other to hand; long rest choose-to-lose moved the picked card to Lost, returned the other to hand, and left the pre-existing lost card lost (no bogus recovery). `tsc` + `oxlint` clean.
- **2026-07-20** Added a **Resources** tracker to the Character tab — a counter per resource for all 9 Frosthaven crafting resources (Lumber, Metal, Hide, Arrowvine, Axenut, Corpsecap, Flamefruit, Rockroot, Snowthistle), in the mat's grid order. New `RESOURCE_TYPES` const + `resources: Record<ResourceType, number>` on `CharacterState`, new `ResourceTracker.tsx` rendering the counters in a responsive grid (1/2/3 columns) with a "N collected" running total in the header. Persists via the existing `fh-character` localStorage entry. Made the tracker defensive against legacy stored characters that predate the `resources` field (reads default to 0, no crash — verified in-browser by seeding a pre-resources `fh-character` and reloading). Also **cleaned up the class-keyword display**: the "· Arcane · Educated · Outcast" bullet-run next to "Human Voidwarden" is now rendered as discrete pill chips instead.
- **2026-07-20** User supplied the actual **Voidwarden character mat photo** (saved to `docs/reference/voidwarden-character-mat.jpg` — see References). This resolved two open Todo items:
  - **Fixed the Dark/Poison perk mislabel**: the mat's icon for "Replace one +0 card with one +1 [icon] card" (1 box) is the same glyph used on the mastery line below it, which reads unambiguously as **Poison**, not Dark — confirming the user's earlier report. `voidwardenPerks.ts` line updated from "Dark card" to "Poison card". The *other* Dark-labeled perk ("Add two +1 Dark cards") uses a visually distinct crescent-moon icon and was already correct — left unchanged.
  - **Seeded the Mastery text** (`src/data/voidwardenMasteries.ts`, wired into `App.tsx`'s seed data): Mastery 1 ("Cause enemies to suffer 20 or more damage in a single turn with granted or commanded attacks") is fully legible, no icons involved. Mastery 2 ("Give at least one ally or enemy Poison, [icon], [icon], or [icon] each round") — only the first condition (Poison) could be read with confidence, by cross-referencing the same glyph against the perk fix above; the other 3 condition icons on the mat are visually distinct from anything else on the sheet and from standard Gloomhaven iconography, so rather than guess at their names they're seeded as `[icon N unconfirmed]` placeholders in the editable text field. **Still needs the user to confirm what those 3 icons are** before the mastery text is fully accurate.

- **2026-07-20** **Fixed the blank-screen crash on the Perks tab** (user report: perk page never renders in their live Chrome, blank screen — but fine in a fresh browser). Root cause: **stale `localStorage` from an earlier schema.** The `Perk` shape has changed three times (`{checked}` → `{boxesRequired, boxesFilled, checked}` → `{timesAvailable, picks[]}`), and `useLocalStorage` trusted whatever JSON was stored, so an old-shaped perk reached `PerkList` which reads `perk.picks.length` — `picks` is `undefined` on old data → `TypeError: Cannot read properties of undefined (reading 'length')`. With no error boundary, the render throw unmounted the whole app (blank white screen); other tabs worked because they don't touch perks. Reproduced exactly by injecting an old `{checked}` perk into the live site's `localStorage`. Fix: gave `useLocalStorage` an optional `sanitize(parsed) => T | null` that runs on load — incompatible data is repaired or discarded (falling back to the seed) instead of crashing the reader. Wired a sanitizer per key in `App.tsx`: seeded reference data (perks/masteries/cards/deck) is shape-validated and re-seeded if stale; the **character sheet is migrated** (merged onto defaults) so real progress survives while newer fields like `resources` are backfilled — the same latent trap the pre-`resources` character data would eventually have hit. Verified in-browser with the exact bad data: Perks now renders the full seeded list and a legacy character kept all values + gained the Resources section, no console errors. `tsc` (build) clean.
- **2026-07-21** **Wired Perks into the Modifier Deck** (Todo item) — picking a perk that adds/removes/replaces a modifier card now actually changes the deck; the two tabs are no longer disconnected. Design: made the deck composition **fully derived** from the perks taken rather than trying to imperatively mutate-and-undo it (which drifts and is ambiguous to reverse). The base 20 plus every picked perk's deck effect *is* the composition, so un-picking a perk automatically reverts its change — one source of truth, no migration of stored deck state.
  - Each Voidwarden perk carries a `deckEffect` in `voidwardenPerks.ts` — the card-kind ids one pick removes/adds (base ids like `minus1`/`plus0`, or perk-only kinds like `frost1`/`poison1`/`dark1`/`heal-shield`/`plus3` defined as `PERK_CARD_KINDS` in `lib/modifierDeck.ts`). Perks with no deck impact (Ignore scenario effects, Grave Defense) omit it. `deckEffectsFromPerks()` flattens perks→effects **by label** (one entry per pick), mirroring how `withVoidwardenCardText` matches by name, so the effects live in one table and retroactive data fixes need no migration. To avoid the U+2212/curly-quote mismatch trap, the effect is co-located on each perk's seed entry and the label→effect lookup is *built from* that array — the strings can't drift apart.
  - `deriveDeckComposition(effects)` folds the effects onto the base counts (clamped ≥0, count-0 kinds dropped); `sameComposition()` compares kind→count order-independently. An `App.tsx` effect keyed on `perks` rebuilds a fresh shuffled deck via `freshDeck(derived)` whenever the derived composition differs from the stored deck's — a no-op for ordinary draws/reshuffles (they don't change composition), and a one-time self-heal on mount for anyone whose stored deck predates this wiring. Perks change between scenarios, so reshuffling there is expected, not data loss.
  - Replaced the Modifier Deck's freeform "Customize deck / Add card type" editor (whose only purpose was to *manually* mirror perk changes — now obsolete, and the freeform editor was slated for removal per the "constrain add-card to real cards" Todo) with a read-only **composition breakdown**: each kind with its count, base cards annotated `−N vs base`, perk-added cards badged **from perk** with an accent-outlined face, a total, and a note pointing to the Perks tab. The drawn-card view also names a perk card when one is drawn.
  - Verified end-to-end in-browser from a cleared seed: base deck = 20; picking "Remove two −1" → `minus1` 5→3, total 18, reshuffled; picking "Replace +0 with +1 Poison" → `plus0` 6→5, `poison1` added (fromPerk), total unchanged; breakdown UI shows the right badges and "18 cards total"; un-picking Poison → `plus0` back to 6, poison gone; reload is stable (no double-apply); and injecting a stale full-20 deck with a perk still picked → mount reconciled it back to 18. `npm run build` (tsc + vite) and `oxlint` clean.
- **2026-07-21** **Guarded against accidental Level changes** (Todo item). The Level counter fed straight into `setLevel`, so a stray tap immediately recalculated Max HP (and clamped Current HP) with no way to catch it — and level is meant to grow into further cascades (deck/cards). Added a **stage-then-confirm** step in `CharacterSheet.tsx`: the Level counter now writes to a local `pendingLevel` instead of the character state, and nothing cascades until the user hits **Confirm**. While a change is staged, an inline bar (reusing the Perks tab's `pick-source-picker` idiom) previews the effect — e.g. "Level 3 → 4 · Max HP 8 → 9" — with **Confirm** / **Cancel**; Cancel drops the draft back to the committed level with zero side effects. The user can step +/− multiple times and confirm once. The existing full-HP-stays-full / otherwise-clamp logic is unchanged, just deferred to Confirm. Verified in-browser: staging level 1→3 left HP at 6/6 (no cascade); Confirm applied it to 8/8 and updated the XP-to-next hint; staging then Cancel reverted the draft with HP untouched. `tsc` clean.
- **2026-07-21** **Major correction: the Voidwarden DOES have level-gated cards, and the app only had 14 of 29.** Earlier entries (2026-07-19) concluded "all 14 cards available from level 1, no per-level unlocking in JOTL" — that is **wrong**, and it's why leveling never offered a card. The user described the real mechanic: at each level 2–9 you add one card to your pool, chosen from that level's two cards *or* a lower-level card you skipped. Verified against the card DB — the class has **29 cards**: the 14 we had (11 marked "1" + 3 marked "X", all available from level 1) plus **15 more at levels 2–9** that were never captured. The original 14-card scan pass simply only ever looked at the level-1/X cards. Did the data half of the fix (data-first, per the user):
  - Pulled the 15 missing scans from the `cmlenius/gloomhaven-card-browser` images branch (same source/approach as the first 14 — read at 2× upscale, paraphrased mechanics-only, scans deleted after use, nothing committed). Added them to `VOIDWARDEN_CARD_DETAILS` in `voidwarden.ts` with a new required `level` field. Level spread (init in parens): **L2** Give and Take (21), Crushing Cold (86); **L3** Commanding Presence (75), Taunting Fate (13); **L4** Savage Instinct (51), The Last Journey (38); **L5** Stand Fast (90) — *lone level-5 card*; **L6** Surge of Power (81), Withering Conviction (44); **L7** Hateful Blast (29), Spirit Hunger (68); **L8** Empowered Link (59), Lull into Oblivion (11); **L9** Possessed by Fate (66), Eye of the Void (19). Persistent halves flagged for the active-area tracker: Commanding Presence top (5-slot → lost, XP 3rd/5th), Empowered Link top (3-slot → lost, XP 1st/3rd), Stand Fast top (Ward, whole-scenario → lost), Surge of Power bottom (whole-scenario → lost).
  - The 14 starting cards got `level: 1` (level-1 and X cards alike, since both are available from the start). **`buildVoidwardenActionCards()` now filters to `level === 1`**, so a fresh character still seeds exactly the 14-card starting pool — the level-2–9 entries are a *catalog* the (not-yet-built) level-up chooser will instantiate from, not owned cards. Verified in-browser: existing character unchanged (14 stored cards, no console errors), and a cleared/fresh seed produces 14 cards all `level: 1`. `tsc` + `oxlint` clean.
  - **Still to do (flow):** the level-up card chooser itself — hook the level-increase Confirm step so it prompts "add one card" from the new level's options (or a skipped lower-level card), instantiating the pick into the pool (Reserve). Also: the Action Cards tab's "All 14 cards are available from level 1" hint is now inaccurate and should change when the flow lands. One thing for the user to sanity-check against their physical cards: **level 5 has only one card (Stand Fast)** where every other level 2–9 has two — that's what the scans show, but worth a glance.
- **2026-07-21** **Built the level-up card chooser (the flow half)** — leveling now actually offers the card pick. The model needs **no new persisted state**: picks owed = `max(0, level − 1) − (leveled cards already owned)`, derived live, so it self-corrects on any level change or add/remove. Pieces:
  - `voidwarden.ts` exports `voidwardenActionCardFrom(detail, status)` (factored out of `buildVoidwardenActionCards`) to instantiate one owned card from the catalog; the chooser uses it with `status: 'reserve'`.
  - `CharacterSheet` gained an `onLevelChange?` callback fired from the level **Confirm**; `App` wires it to `setTab('Action Cards')` so confirming a level change jumps straight to where the pick/removal lives. `ActionCards` now takes `characterLevel`.
  - **Chooser** (`owedPicks > 0`): an accent-bordered panel listing the catalog cards with `level` in 2..currentLevel that you don't already own — i.e. the new level's cards *plus* any lower-level card you skipped — each with level/init badges, top/bottom text (loss 🔥 marked) and an **Add** (→ Reserve). "Choose later" collapses it to a one-line reminder; it re-expands on remount (switching tabs), so a deferred pick keeps nagging until spent.
  - **Over-cap remover** (`owedPicks < 0`, i.e. you leveled *down* below where a card was gained): a danger-bordered panel offering to remove a leveled card (two-step Remove/Cancel confirm) to hand the pick back — chosen behavior per the user, non-destructive by default.
  - Updated the stale Reserve hint ("All 14 cards available from level 1") to describe the 3 "X" cards + added level-up cards.
  - Verified the whole loop in-browser from a fresh seed: at level 3 the chooser showed "2 to choose" (the 2×L2 + 2×L3 cards); Add dropped Give and Take into Reserve and decremented the count; at 0 the panel vanished; confirming level 3→4 auto-switched to the tab and re-opened the chooser now offering the skipped L3 cards **and** the new L4 cards (not the owned L2 ones); confirming level 4→2 auto-switched and showed the over-cap remover ("1 beyond what level 2 grants"), whose two-step Remove cleared it. No console errors; `npm run build` (tsc + vite) and `oxlint` clean.

- **2026-07-21** **Constrained the Action Cards "+ Add card" to the real card pool** (Todo item — the remaining half; the Modifier Deck half was already done). Replaced the freeform add form (typed name/level/initiative/top+bottom text/loss toggles/tag picker — which let you invent arbitrary cards) with a **catalog picker**: "+ Add card" now lists every Voidwarden card unlocked at your current level (`level <= characterLevel`) that you don't already own, each with L/init badges and its paraphrased top/bottom text, and an **Add** that drops it into Reserve. This reuses the exact markup/CSS the level-up chooser already uses (`level-pick-list` / `action-card level-pick-card` / `lvl-badge` / `init-badge` / `side-static`), and a new `addableCards` memo + shared `addFromCatalog(name)` helper that mirror the already-working `availableLevelCards` / `addLevelCard`. The picker is a *superset* of the level-up chooser's list — it also covers re-adding a level-1 card removed by accident (the exact recovery the old freeform form was used for in an earlier session). The derived `owedPicks` accounting reconciles automatically: adding a leveled card this way spends a pick, adding beyond your cap surfaces the over-cap remover. Removed the now-dead `draft`/`emptyDraft`/`addCard`/`toggleTag` state and the orphaned `.card-form` / `.card-form-row` / `.loss-toggle` CSS (kept `.tag-picker`/`.tag-chip`, still used by the rest pickers); dropped the unused `CardTag`/`CARD_TAGS` imports. `tsc`, `oxlint`, and `npm run build` all clean. **In-browser verification was blocked this session** — the Browser preview stayed stuck on "Policy check in progress" / navigation denied for the whole turn, so this rests on the static checks plus the fact that the picker is structurally identical to the already-verified level-up chooser; worth a quick in-app glance next session.

- **2026-07-21** **Full icon-correctness audit of every card, perk, and mastery, then rendered game terms as inline emoji glyphs** (user request: "make sure every icon shown on a card is correct in text form" + "I'd like a visual icon"). Two parts:
  - **Audit (data-first).** Re-pulled all 29 Voidwarden card scans (`cmlenius/gloomhaven-card-browser` images branch — note the path double-nests `images/images/…`; also found `data/character-abilities/jaws-of-the-lion.json` there, which is metadata-only (name/level/init, all 29 matched us) but has no ability text, so icons still had to be read from the scans). Read each at 2× with per-icon zoom on every element/condition, deleted the scans after. **All 14 level-1 cards were already correct.** Found and fixed a **systematic Fire→Dark misread in the level-2–9 set**: all six "Consume Fire" instances (Commanding Presence, Withering Conviction, Hateful Blast ×2, Lull into Oblivion, Possessed by Fate) are actually **Consume Dark** — the dark crescent-moon orb, not a flame — which fits the class (the Voidwarden's kit is Dark/Ice and never *consumes* Fire; it only *infuses* Fire once, on Give and Take, which was already right). The init-flanking "starburst 2 / leaf 2" hex badges read a constant value across cards and don't match the actual action numbers — a template artifact, ignored.
  - **Audit of the mat** (`docs/reference/voidwarden-character-mat.jpg`) fixed several perk/mastery icons: perk "Replace one −1…" replacement card is **+0** (was +1) and its second icon is a **Target** bullseye, not **Shield** (both heal perks now read "Heal 1, Target 1 ally"); the "+1 Frost" perk is actually **+1 Curse** (bolt-in-diamond, not the snowflake — that's the separate "Add two +1 Frost" perk); **Grave Defense** gives **Ward** to an ally who has **Poison** (was "give Dark … who has Dark"). Identified Mastery 2's three previously-unconfirmed icons as **Strengthen, Regenerate, Ward** (Ward = the same shield/flame glyph as Grave Defense's give-icon, cross-confirming). The perk deck-wiring followed: `modifierDeck.ts` `PERK_CARD_KINDS` gained `heal-target0` (+0), `heal-target1` (+1), and `curse1`, replacing the mislabeled `heal-shield`; perk `deckEffect`s point at the new kinds. Because perks/masteries persist in localStorage (and the deck is derived from perk *labels*), added idempotent one-time migrations `withVoidwardenPerkFixes` / `withVoidwardenMasteryFixes` (old-label → new-label maps, wired into `App.tsx`'s mount effect next to `withVoidwardenCardText`) so a stored character picks up the fixes without losing picks/achieved flags — the deck then re-derives itself.
  - **Emoji rendering.** New `src/lib/gameIcons.ts` (a collision-free term→emoji map + a case-sensitive, word-boundaried matcher that splits a paraphrase into text/icon segments) and `src/components/CardText.tsx` (renders each recognized term as `emoji + word`, emoji `aria-hidden`). Case-sensitivity is the trick: our paraphrases capitalize a word only when it means the game concept, so "Attack 3" / "Consume Ice" decorate but "attack modifier deck" / "move through" don't (verified by running the real module through esbuild/node against sample strings). The card **loss** marker moved from `🔥` (which now means the Fire element) to **`❌`**, matching the red X the cards actually print. Wired `CardText` into all card-text sites in `ActionCards.tsx` (hand side-buttons, active-effects text + note, level-up chooser, the "+ Add card" picker), perk labels in `PerkList.tsx`, and — since masteries render inside an editable `<input>` — a read-only decorated **preview line** beneath each mastery input in `MasteryList.tsx`. All glyph knowledge lives in those two files, so swapping emoji for custom SVGs later needs no data change. `tsc`, `oxlint`, `npm run build` all clean.
  - **Still to confirm (user):** the **Regenerate** and **Ward** readings on Mastery 2 (and the matching Grave Defense **Ward** give-icon) are a confident best-read of the mat, not printed words — worth a glance against the physical mat. **In-browser verification was blocked all session** (the Browser preview stayed stuck on "Policy check in progress" / navigation denied), so the visual pass rests on the static/build checks plus the executed-module test above rather than a screenshot.
- **2026-07-21** **Replaced the emoji glyphs with the user's own painted icon art.** The user created a "Combined Voidwarden's Glyph Lexicon" sheet (saved to [`docs/reference/glyph-lexicon.png`](docs/reference/glyph-lexicon.png)) with a glyph for all 26 game terms the app uses — including distinct **Shield** (plain shield) and **Ward** (shield + eye) so the two read differently, plus **Jump**, **Move**, and an **XP** pentagram. Sliced the 26 unique cells (skipping the duplicate/white-masked cells) into `public/icons/*.png`. Kept them as **small parchment tiles** rather than transparent cutouts: the art is painted with soft glows that bleed into the parchment and several glyphs are tan/brown (Bless card, Loot bag, boot), so background-keying leaves halos and eats the light glyphs — tiles are clean-edged and consistent instead. `gameIcons.ts` now maps each term to `/icons/<term>.png` (the matcher/segmenter is unchanged); `CardText` renders an `<img class="game-icon-img">` + the word; `.game-icon-img` sizes them to 1.45em with a 4px radius. The `LOSS_ICON` stays the ❌ emoji (a UI marker for a lost half, not a lexicon glyph). Verified in-browser (the preview gate finally cleared): Action Cards and Perks tabs show the tiles inline in real card/perk text — e.g. Signs of the Void's "🛡Shield 1 … 👢Move 2", the corrected "+0 ❤Heal 1, 🎯Target 1 ally" and "+1 🃏Curse card" perks — with **no console/network errors** (all 26 tiles load). `npm run build` + `oxlint` clean. Swapping the art later (e.g. transparent versions) is just re-slicing into `public/icons/`; no code/data change.
- **2026-07-21** **Upgraded to the user's full illustrated icon set, and put the icons on the Resources tracker and Modifier Deck.** The user replaced the glyph-lexicon with a much richer painted sheet, [`docs/reference/Frosthaven Icons.png`](docs/reference/Frosthaven%20Icons.png) (glyph-lexicon.png removed) — full circular tokens for status afflictions, boons, core actions, card-duration, all 9 crafting resources, and the attack-modifier cards. Work across several passes as the user iterated on the sheet:
  - **Slicing.** A PIL script auto-detects each row's icons by column gaps in a saturation/darkness mask (the icons are dense but separated by clean parchment), crops uniform tiles, and names each file by its printed label (spaces→hyphens; `+1`→`plus1`, etc.). Duplicates are kept losslessly with a `-2` suffix. The full set (63 tiles) lands in [`docs/reference/icons/`](docs/reference/icons/) as a reviewable staging area; only the *used* names are copied into `public/icons/`. Crucially the copy **preserves the 7 element tiles + `xp`** already there (that sheet has no elements), overwriting/ adding everything else.
  - **attack/range/target** appear in two visual variants on the sheet; the user picked the **actions-row set** (crossed swords / bow+arrow / group crosshair — the standard Gloomhaven ability look) as canonical for card text. The boons-row variants stay in the staging folder unused.
  - **Modifier row iterations.** The first sheet mislabeled the −1 token as "MULTIPLIER" and had promo "NEW" ribbons on some cards; the user re-exported to fix both, then exported once more adding **+0 and +3** tokens (the base deck's most-common card and a perk card, which the earlier sheet lacked). That last export was half-resolution (1408×768 vs 2816×1536), so `plus0`/`plus3` were sliced and **upscaled to 190px** to match the crisper tokens.
  - **Resources tracker** — `Counter` gained an optional `icon` prop (renders a small round tile before the label); `ResourceTracker` passes `resourceIcon(name)` (new helper in `gameIcons.ts`; the 9 `RESOURCE_TYPES` map 1:1 onto lowercase filenames). All 9 tokens verified loading (200s, natural 190px).
  - **Modifier Deck** — new `modifierIcon(value)` maps a card's display value (`+0…+3`, `x2`, `Null`) to its token; `ModifierDeck` renders the token on the drawn card face and in every composition row, with a **text fallback** for anything unmapped. CSS: `.card-face-img` fills the card, `.deck-comp-face-img` sits inline, and `.deck-comp-face:has(img)` drops the old text-box chrome around a token.
  - **Perk-card element badges** (user request: cards like "+1 Poison" should show the element, not just the number). Added `icons?: string[]` to `ModifierCardType`; tagged the perk cards in `modifierDeck.ts` (Poison/Curse/Frost/Dark each their element, both Heal/Target cards `['Heal','Target']`). Rendered as small glyphs (via `GAME_ICONS`) tucked into the drawn token's lower-right corner and inline on the composition row — matching how the physical perk cards print a number with an element symbol. Verified by picking the Poison/Frost/Heal-Target perks and drawing until each surfaced.
  - Verified each piece in-browser (resource row, drawn `+0`, composition list with element badges on the perk cards); test perks/deck state cleaned out of `localStorage` afterward. `tsc -b` + `oxlint` clean throughout.
- **2026-07-21** **Added the user's painted element art (the 6 elements).** New sheet [`docs/reference/Frosthaven Elements.png`](docs/reference/Frosthaven%20Elements.png) — sliced the 6 **vibrant elements** (`fire, ice, air, earth, light, dark`) and replaced the old placeholder element tiles in `public/icons/`. The parchment texture defeated projection-based row detection, so the vertical center was pinned by tracing the fire icon's rim along its center lines. **Frost** has no dedicated tile (it's the Voidwarden's name for the **Ice** element), so `frost.png` is aliased to the new Ice crystal art — Frost and Ice now render identically, which is correct. These flow everywhere via `gameIcons.ts` automatically; verified in the Perks text ("Add two +1 ❄️ Frost cards", "+1 💀 Dark cards", "Grave Defense: consume ❄️ Frost + 💀 Dark…") and the modifier-deck perk badges — 16 element/effect glyphs, none broken. `tsc -b` + `oxlint` clean. The sheet also carries **13 "waning residue" variants and 6 "core interaction" icons** (Infusion, Consumption, Waning, Chaos, Resonance, Elemental Reaction) not yet extracted — available if card text ever needs them.
- **2026-07-21** **Closed the editable sets and swapped the XP tile for an emoji star** (user request — "everything needed should already be there"). The action cards, perks, and masteries are fixed Voidwarden content, so the free-add affordances were pure risk (accidental junk / drift from the real components). Removed: the Perks "Add a perk…" form and per-perk delete ×; the Mastery "+ Add mastery" button and per-row delete ×; the Action Cards standalone "+ Add card" catalog picker (and its `showAdd`/`addableCards`/`removeCard` plumbing) plus the per-card delete ×. **Kept** the legitimate state changes: perk box pick/undo, mastery text + achieved toggle, card status swaps (Reserve/Hand/Used/Lost), and — crucially — the **level-up card chooser** and **level-down over-cap remover**, since gaining a card on level-up is a real mechanic, not an arbitrary add (the two share `addFromCatalog`/`availableLevelCards`, left intact). Verified in-browser: all three tabs lost their add/remove buttons while keeping pick boxes (17), mastery inputs/toggles (2/2), and card status swaps. `tsc -b` + `oxlint` clean, no console errors.
  - **XP glyph — briefly an emoji, then back to a tile.** First pass replaced the placeholder `xp.png` with an emoji star (a `TERM_EMOJI` map in `gameIcons.ts` parallel to the image map, an emoji branch in `CardText`) since the sheet had nothing for XP. The user then added a **Progression & Experience** section to `Frosthaven Elements.png` (Experience / Waning Experience / Level Up), so that got reverted: sliced the gold **Experience** star into `public/icons/xp.png`, restored `XP → xp` in the image map, and dropped the `TERM_EMOJI`/emoji-segment code. That sheet re-export was half-resolution (1407×768), so the star was upscaled to 190px. Verified XP now renders as the gold star tile inline (11 instances, none broken). *(Note: `Frosthaven Elements.png` also swapped its old Chaos/Resonance/Elemental-Reaction interaction icons for the three progression ones — those three interactions are gone from the current sheet.)*

## References

External sources consulted while building the Voidwarden companion, for anyone picking this up later:

- **[m-ender/gloomhaven-rules](https://github.com/m-ender/gloomhaven-rules)** — full transcription of the official Gloomhaven rulebook, both text-only (`README-no-images.md`) and with diagrams (`README.md`, images under `img/rulebook/`). The authoritative source for core mechanics (leveling, perks, Battle Goals, attack modifier deck, hand-building). When fetching it, prefer downloading the raw markdown directly and grepping rather than a summarizing fetch — the file is large enough that summarization tools have dropped whole sections. **Worth pulling the actual diagrams too** (e.g. `img/rulebook/character-sheet.png`, `img/rulebook/battle-goal-card.png`) — the text has callout markers like "(a)", "(b)" that reference specific parts of a diagram and don't mean much without seeing it.
- **[gloomhavencards.com/jotl/characters/VW](https://www.gloomhavencards.com/jotl/characters/VW)** — Jaws of the Lion Voidwarden card browser. Used to verify actual card names, initiative values, and the "1"/"X" level markers (card art is rendered as images, not readable via a plain text fetch — had to navigate the actual image URLs and zoom in with the browser tool to read them). Good for exact card stats; its "Build Mode" does *not* enforce real unlock rules, so don't infer game mechanics from what it lets you click.
- **[cmlenius/gloomhaven-card-browser](https://github.com/cmlenius/gloomhaven-card-browser) images branch** — the card-scan image set that gloomhavencards.com serves (`images/character-ability-cards/jaws-of-the-lion/VW/jl-<card-name>.jpeg`). Used as read-only reference to fill in the Action Card summaries: fetch to scratchpad, read, crop+upscale ambiguous icons (consume vs infuse, which element) with PIL, then delete. **Do not commit these scans to the repo** — they're Cephalofair's copyrighted card art; the app stores only paraphrased mechanics summaries.
- User-supplied photos of the physical **Voidwarden character mat** and **perk sheet** — the ground truth for HP-by-level, XP thresholds, hand size, and the exact perk list/box counts, and the source of two corrections to earlier assumptions (see entries above). The character mat image itself is saved at [`docs/reference/voidwarden-character-mat.jpg`](docs/reference/voidwarden-character-mat.jpg) so it's available for future reference without needing the user to re-send it.
- User-created **icon art sheets** — [`docs/reference/Frosthaven Icons.png`](docs/reference/Frosthaven%20Icons.png) (conditions, boons, actions, card-duration, resources, attack modifiers) and [`docs/reference/Frosthaven Elements.png`](docs/reference/Frosthaven%20Elements.png) (the 6 elements + waning-residue + core-interaction icons). These are the source art behind `public/icons/*.png`; the full sliced set (named by printed label, duplicates suffixed `-2`) is staged in [`docs/reference/icons/`](docs/reference/icons/). To change the art, drop a new sheet in and re-slice — no code/data change. These replaced the earlier single-sheet `glyph-lexicon.png`.

## In Progress

_(nothing yet)_

## Todo

- **2026-07-20** User-reported batch, from reviewing the app against the physical materials:
  - ~~Add the class keywords **Arcane · Educated · Outcast** next to "Human Voidwarden" on the Character tab~~ — done, see Done section (rendered as pill chips from the mat's Notes section).
  - ~~Add a **Mastery** section to the Perks page~~ — done, see Done section. Text is seeded from the mat photo now, but Mastery 2 still has **3 unconfirmed condition icons** (`[icon N unconfirmed]` placeholders) that need the user to identify before it's fully accurate.
  - ~~**Fix wrong perk data**~~ — done, see Done section (confirmed Poison via the mat photo, fixed `voidwardenPerks.ts`).
  - ~~Add a **Resources** reference to the Character tab~~ — done, see Done section. Counter per resource for all 9 crafting resources with a collected total.
  - ~~**Modifier Deck looks broken**~~ — fixed, see Done section (reshuffle-drops-cards bug).
  - ~~**Wire Perks into the Modifier Deck**~~ — done, see Done section (deck composition is now derived from picked perks).
  - ~~**Guard against accidental Level changes**~~ — done, see Done section (Level counter now stages its value and requires an explicit Confirm before the HP cascade applies).
  - ~~**Action Cards are missing top/bottom text**~~ — done, see Done section (paraphrased summaries for all 14 cards from the card scans, with verified loss flags and tags).
  - ~~**Level changes should prompt an action-card choice**~~ — done, see Done section. The earlier "no per-level gating" assumption was wrong; added the 15 level-2–9 cards and a level-up chooser (add one card from the new level or a skipped lower level → Reserve) plus a level-down over-cap remover, all driven by a derived "picks owed" count. *(The lone level-5 card, Stand Fast, is not a data gap — the user pointed to the rulebook (m-ender/gloomhaven-rules p.44), which confirms leveling adds "one new card ... level equal to or less than the character's new level," with no requirement that every level print two cards. The chooser already offers any unowned card of level ≤ current, so a single-card level works correctly.)*
  - ~~**Constrain "add card" everywhere to real cards, not freeform text**~~ — done, see Done section. Action Cards' "+ Add card" is now a picker over the real Voidwarden pool (filtered by level availability, minus cards already owned), replacing the freeform name/level/init/text/tags form. Both halves of this item are now complete.
- **2026-07-21** **Apply status effects (conditions) to the character, with correct removal mechanics.** Add a way to mark conditions on the Voidwarden and have the app enforce *when each one comes off* per the rulebook, rather than leaving it to the player to remember:
  - **Round conditions** (Stun, Immobilize, Disarm, Muddle — and the positive Strengthen, Invisible, Regenerate, Ward) expire at the end of the affected figure's **next turn**. Needs an end-of-turn / next-turn trigger to auto-clear them.
  - **Wound** — suffer 1 damage at the **start** of the figure's turn; removed by any **Heal**.
  - **Poison** — no timed expiry; the next time the figure is **Healed**, the Poison is removed *instead of* restoring HP (the heal is consumed).
  - **Bane** (Frosthaven) — suffer 10 damage at the start of the turn, then Bane is removed.
  - **Brittle** (Frosthaven) — the next source of damage is doubled, then Brittle is removed.
  - **Regenerate** (Frosthaven) — Heal 1 at the start of each of the figure's turns while it lasts (until its round-condition expiry).
  - Curse/Bless aren't figure conditions here (they're modifier-deck cards — already modeled in the Modifier Deck tab). Implementation should tie into the Character tab's HP tracking (Wound/Bane/Poison-vs-Heal interactions all touch current HP) and reuse the verified condition set + emoji glyphs from `gameIcons.ts`. Get the removal timing right first (a "start turn" / "end turn" advance control is probably the cleanest driver), then the visual.
- ~~**2026-07-21** **Remove the ability to add arbitrary entries — action cards, perks, masteries are all closed sets.**~~ — done, see Done section. Stripped the "+ Add card" catalog picker, the "Add a perk…" form, and the "+ Add mastery" button (plus the per-item delete ×'s), keeping the level-up card chooser / over-cap remover and the pick/undo/status toggles. Also swapped the XP tile for an emoji ⭐.
- Decide on real-time party-state architecture if/when this needs to sync across multiple players' devices — likely a Durable Object per campaign/session, deferred since the current scope is single-character/single-device.
- Broaden beyond the Voidwarden to other classes/characters if the group wants it on more than one device.
- **Once the mechanics above are complete and tested, do a pass on the final layout/visual design** — sequencing note from the user: get the game logic right first, polish presentation after.

## Notes / Decisions

- Chose **Workers** (not Pages) as the deploy target because Durable Objects (needed for real-time multiplayer state later) aren't available on Pages, and Cloudflare's native Git integration (Workers Builds) now supports Workers directly, not just Pages, so there's no tradeoff.
- Used `@cloudflare/vite-plugin` for a single-Worker full-stack setup (frontend assets + API in one deploy) rather than React Router framework mode, keeping the stack simpler for now.
- `npm create cloudflare@latest` (C3)'s interactive prompts hung non-interactively even with `--accept-defaults`/`CI=true` (variant-selection step doesn't respect flags in v2.70.12) — worked around by scaffolding with plain `npm create vite@latest` and manually wiring in `@cloudflare/vite-plugin`, `wrangler`, and `hono`.
- **Perk list, action card text/initiative, and HP-by-level are intentionally left for the user to enter**, not hardcoded from research — couldn't reliably verify Cephalofair's exact numbers/wording from available sources, and didn't want to either assert wrong game data as fact or reproduce their proprietary card text into a public repo. The user's physical components are the authoritative source anyway.
- Found and fixed a real stale-closure bug during testing: rapid-fire clicks on the deck draw button (simulated via browser automation faster than any human tap) could drop draws because `onChange(computeNext(state))` closed over a stale `state`. Fixed by switching all list/deck mutations (`ModifierDeck`, `PerkList`, `ActionCards`) to functional `setState` updates (`onChange(prev => ...)`) so each update reads the latest state regardless of dispatch timing.
